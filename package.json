{
  "name": "osher-finance-app",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter @osher/web dev",
    "build": "pnpm --filter @osher/web build",
    "start": "pnpm --filter @osher/web start",
    "lint": "pnpm --filter @osher/web lint",
    "typecheck": "pnpm --filter @osher/web typecheck"
  },
  "engines": {
    "node": ">=20.11.0",
    "pnpm": ">=9.0.0"
  }
}
